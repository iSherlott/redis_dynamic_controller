export enum MGlobal {
    SUCCESS = "API in developement",
    FAILED = "Error response",
    INTERNALERROR = "Internal Error"
}

export enum MDefaultController {
    ROUTERNOTFOUND = "Router not found",
    ERRORROUTERNOTFOUND = "Error trying to locate the router"
}

export enum MStrutureResponse {
    INTERNALERROR = "Internal Error"
}

export enum MHomeController {
    ERRORDATACONFIG = "Error getting api configuration data"
}